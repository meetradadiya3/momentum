#include<stdio.h>

main()
{

   int a=50,b=105,c=102,d=100;
   (a>b)? (a>c)? (a>d)? printf("a is max") : printf("b is max") : (c>d)? : printf("c is max") : printf("d is max")`:
   (b>c)? (b>d)? printf("b is max"):printf("d is max"):(c>d)?printf("c is max"):prints("d is max");
   	
}
